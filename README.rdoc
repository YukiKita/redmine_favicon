= Redmine favicon

Redmine favicon is a tiny plugin for Redmine which adds favicon to its base layout.

== Installation and Setup

1. Make sure the plugin is installed to +vendor/plugins/redmine_favicon+
2. Restart Redmine

